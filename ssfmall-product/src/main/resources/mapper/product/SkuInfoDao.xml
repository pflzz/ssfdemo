<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pfl.ssfmall.product.dao.SkuInfoDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.pfl.ssfmall.product.entity.SkuInfoEntity" id="skuInfoMap">
        <result property="skuId" column="sku_id"/>
        <result property="spuId" column="spu_id"/>
        <result property="skuName" column="sku_name"/>
        <result property="skuDesc" column="sku_desc"/>
        <result property="catalogId" column="catalog_id"/>
        <result property="brandId" column="brand_id"/>
        <result property="skuDefaultImg" column="sku_default_img"/>
        <result property="skuTitle" column="sku_title"/>
        <result property="skuSubtitle" column="sku_subtitle"/>
        <result property="price" column="price"/>
        <result property="saleCount" column="sale_count"/>
    </resultMap>
    <resultMap id="SkuItemSaleAttrVo" type="com.pfl.ssfmall.product.vo.SkuItemSaleAttrVo">
        <result column="attr_id" property="attrId"></result>
        <result column="attr_name" property="attrName"></result>
        <collection property="attrValues" ofType="com.pfl.ssfmall.product.vo.AttrValueWithSkuIdVo">
            <result column="attr_value" property="attrValue"></result>
            <result column="sku_ids" property="skuIds"></result>
        </collection>
    </resultMap>
    <select id="getSkuItemSaleAttr" resultMap="SkuItemSaleAttrVo">
        SELECT
            ssav.`attr_id` attr_id,
            ssav.`attr_name` attr_name,
            ssav.`attr_value` attr_values,
            GROUP_CONCAT(DISTINCT info.sku_id) sku_ids
        FROM `pms_sku_info` info
                 LEFT JOIN `pms_sku_sale_attr_value` ssav ON ssav.`sku_id` = info.`sku_id`
        WHERE info.spu_id = #{spuId}
        GROUP BY ssav.`attr_id`, ssav.`attr_name`, ssav.`attr_value`
    </select>


</mapper>